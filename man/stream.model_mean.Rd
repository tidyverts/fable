% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mean.R
\name{stream.model_mean}
\alias{stream.model_mean}
\title{Extend a fitted mean model with new data}
\usage{
\method{stream}{model_mean}(object, new_data, specials = NULL, ...)
}
\arguments{
\item{object}{A model for which forecasts are required.}

\item{new_data}{A tsibble containing the time points and exogenous regressors to produce forecasts for.}

\item{specials}{(passed by \code{\link[fabletools:forecast.mdl_df]{fabletools::forecast.mdl_df()}}).}

\item{...}{Other arguments passed to methods}
}
\description{
Applies a fitted average method model to a new (immediately subsequent)
portion of data, updating the model's fitted values, residuals and \code{sigma2}
without re-estimating the mean. Unlike \code{\link[=refit.model_mean]{refit.model_mean()}}, \code{stream()}
does not need to reprocess the entire history, only the newly provided
observations, making it well suited to incrementally updating a model as
new observations arrive.
}
\details{
For a fixed mean model, the \code{.fitted}, \code{.resid} and \code{sigma2} values
returned by \code{stream()} are identical to those obtained by refitting the
model (with the same fixed mean) to the complete series.

For a rolling window mean model (specified with the \code{window()} special),
the rolling mean is continued across the new observations. The fitted values
and residuals are identical to those obtained by estimating the model on the
complete series, and the most recent rolling mean is retained for
forecasting.
}
\examples{
lung_deaths_male <- as_tsibble(mdeaths)

fit <- lung_deaths_male \%>\%
  filter(index < yearmonth("1979 Jan")) \%>\%
  model(MEAN(value))

fit \%>\%
  stream(lung_deaths_male \%>\% filter(index >= yearmonth("1979 Jan"))) \%>\%
  report()
}
